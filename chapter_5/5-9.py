usernames = ['admin', 'lue', 'getzs', 'eval', 'samg']

for username in usernames:
    if usernames:
        for username in usernames:
            print(f"Valid usernames are {username}")
    else:
        print("We need to find users!")
